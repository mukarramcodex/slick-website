export interface TimelineItem {
  year: string;
  title: string;
  description: string;
  image: string;
}

export interface CompanyTimelineProps {
  // No specific props needed for the CompanyTimeline component
}