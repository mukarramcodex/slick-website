export interface TeamMember {
  name: string;
  position: string;
  image: string;
  bio: string;
  expertise: string[];
}

export interface TeamProps {
  // No specific props needed for the Team component
}